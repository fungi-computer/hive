# Actual Clearing environmental geometry — source join in progress

This production producer replaces the original 32-cell wet-study direction. It
queries the existing height/cave world, includes the whole playable 15×15 map
inside a one-cell collar, and retains actual disconnected underground voids.
It has no flat foundation or handpicked dig coordinate. Original soil is found
by the generated material query after feature carving; a height label alone
cannot create porous stock. The registered content supplies initial moisture
once. Existing water rebind owns spoil export and dry newly excavated space.

The shared physical geometry query supplies completed solid volumes and faces.
The water producer classifies porous soil before testing impermeable solidity,
and removes only actual blocked openings. Player placement remains inside15×15;
the environmental query advertises the collar independently. The finite outer
lateral/bottom water condition is explicit and creates no external source. It
does not claim those edges are solid cave walls or supply gas boundary metadata.

Coverage includes the registered underground range and enough upper cells for
the highest actual source/structure plus its real dry neighbor. The host retains
a monotone coverage ceiling; removing a building does not delete that coverage.
The dry open top is a water-work truncation, not the atmosphere's ambient plane.
No unsimulated global fine world is generated. Current implementation queries the
finite region on geometry changes and caches point reads privately; its cold cost
and active tick cost still need actual measurement.

The old 2,048-cell and 65,536-data-node protective defaults cannot describe this
consumer. Water admission now takes explicit bounded limits; ordinary Region
codec callers retain their current structural default. Goblin declares limits
covering the possible17×17×128 address envelope, omitting impermeable solids.
Raising an admission bound proves no performance capacity. The existing per-call
face/path work budget remains; the host must use its real small fixed interval,
not replay six seconds as one expensive field operation.

Three actual-generator laws are authored, not run. No full Clearing caller,
material/pail/schema/clock/render or new gas join is accepted in this source
checkpoint. Original 985849f water laws17/17 plus one shared arithmetic law and
strict app types passed u4658; Fallow ended1 with retained introduced findings.
These producer/admission changes postdate that result and need their affected
qualification. No speed, rendered, native-DO or hosted claim follows.
