/** Shared physical scale for Colony content. Keep this dependency-free so
 * actor definitions never import the compiled environment that consumes them. */
export const COLONY_VERTICAL_METRES = 0.54;
