import { component } from "./authoring";

/** Native marker for a positioned, finite material pile released by excavation. */
export const GroundStock = component<{}>("hive.ground-stock", {
  version: 1,
  fields: {},
});
