# Animal study proof v2

Run from `/home/levi/src/hive` with the existing bounded proof runner:

```sh
bash /home/levi/src/Botanical-next/.agents/skills/orchestrate-multi-lane-work/scripts/run-proof.sh \
  node .botanical/animal-study/prove.mjs .botanical/animal-study/proof-v2
```

The WebM is an approximately 8-second pasture-canvas motion capture after
startup, with timed programmatic pose and pack controls. The same proof uses
Playwright control interactions separately for the asserted UI behavior.

Final bounded run: systemd scope `run-u576.scope`, native exec session `63400`,
normal exit `0`.
