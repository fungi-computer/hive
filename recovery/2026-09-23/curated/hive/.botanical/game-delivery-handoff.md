# Game delivery lead — first working handoff, 2026-09-07

You are the visible Sol `game-delivery` lead for Levi's Hive game. Astra is
your peer Game CTO/product/art contact, Herdr session `botanical`, agent
`goblin-bnb`, thread `01a0791e-7ac8-7cc0-90dd-48f8d164e526`, pane `w1:pV`.
Verify the live name before sending concise `agent-report game-delivery` messages.
Routine delivery does not report to Botanical CTO or its delivery-lead.

Levi authorized this role: own ready-work selection, file custody, PM briefs,
early source/caller review, corrections, routine final acceptance, proof
scheduling, serial integration and publication on the SAME feature preview.
Make those decisions; do not turn every routine check into another Astra gate.
Astra retains product direction, hard architecture decisions and PERSONAL review
of changed original 3D art at native/game scale and in motion. Batch that art for
review while unrelated source/proofs proceed. Use existing issues/docs/logs.

Create one persistent delivery goal for completing the already-authorized
two-person home, visitor study regression and coherent same-preview delivery;
no invented token budget. Root's current game goal remains active separately.
Finish means actual recruit/select/direct/queued/shared work by two visible
people, conserved scarce material and cancellation, shared home/rest, Bramble
guidance, normal/narrow input/focus/pause/reset/replay, dist and hosted proof.

## Initial custody — read-only until the explicit transfer message

Verify pwd/Git root/branch/HEAD/status first. Checkout `/home/levi/src/hive`, branch
`feat/goblin-bed-and-breakfast-mvp`, HEAD and remote/live baseline
`016b1a02b009e798165bd4ba11653093cf4ee1c6`. It has valuable DIRTY source; never
reset/switch/rebase/clean it. Astra is finishing one rest-order correction in
the coupled typed simulation and retains Git/deploy for this short checkpoint.
Read/coordinate now. Do not mutate source, dependencies, Git, dist or provider
until Astra explicitly transfers custody after the current check settles.

Astra's native Sol reviewer currently owns ONLY `src/clearing.test.js` for the
rest correction. A native Luna reader owns future-direction paragraphs in
PROTOTYPE/ARCHITECTURE and ignored issue drafts #4/#10/#13. Astra owns all other
dirty paths, including art. They will settle before transfer. Your first real
reply should acknowledge the exact boundary and name the actual source/caller
gap you will advance after transfer; a send receipt alone is not acknowledgment.

## Read the actual candidate, then decide the smallest working UI integration

Read PROTOTYPE.md and ARCHITECTURE.md, current package/main/hud/view/keys/camera,
and the typed core. Existing public issues are the queue:
https://github.com/fungi-computer/hive/issues/1 (two-person home),
/issues/2 (original visitor study), /issues/12 (Caps and colony controls).
Later floors, chunks/caravans, gear, water and occult cards must remain recorded
without entering this candidate.

The real current path: main.request emits scoped commands; clearing.ts.step
admits them; orders.ts records intent; jobs.ts admits each idle person's first
READY personal order before shared party work; matching.ts calls the actual
libcolony optimizer once over the eligible rectangle; claims reserve real wood
before movement; activity.ts transfers piles -> actor cargo -> site exactly once.
Personal direct commands interrupt and preserve unfinished prior orders. Shared
designations use `{party:"home", actors:null}`, personal queues capture actor IDs.
Recruit command joins existing visible-intended Sedge to home once.

The model now has `actors.rowan`, `actors.sedge` and `parties.home.members` (initial
Rowan only); no `state.pawn`. Sedge is an adult human, figure `witch-runner`,
waiting at (10,12). Current main/view/HUD were mechanically adapted to Rowan,
so Sedge is NOT yet visible/recruitable and this dirty source is NOT publishable.
You need actual roster/visitor/recruitment, selection of either/both, contextual
direct vs queued orders and Chop click-drag designation. Preserve controlled
pawn semantics, automatic travel/action and existing world/task authority.

Levi selects Caps AS-IS. Read the actual packages and probe:
`.botanical/caps-probe/README.md`, actual probe script/page and tarballs;
`/home/levi/src/Botanical-next/packages/caps` and `packages/stipe` READ-ONLY.
Their native packed tarballs are available locally. Caps peers React18; Hive19.
React18.3.1 satisfied the isolated runtime probe; ordinary local dependency
integration is authorized. Decide the smallest reproducible consumption from
the real package, without modifying Botanical or forking widgets. Root read
Button/Checkbox and approves this host dependency direction, but not a claim
that it has shipped. Existing OpenTUI keymap remains keyboard owner. NOTE probe
README overclaims forwarded-ref focus and disabled activation: script only uses
locator.focus and the disabled button has no handler. Correct/replace those
claims with actual joined-game focus proof; don't bless vacuous tests.

## Existing source review and evidence

Root personally authored typed actors/resources/activity/routine/jobs/orders/core
and matching.ts, deleting old single-person clearing/jobs/resources owners.
Native Sol ran actual16MiB WASM 14 laws, run-u278.scope exit0. It caught and root
fixed personal direct priority and native sparse-matrix padding failures. Read
matching.ts and actual vendor to understand the finite forbidden-pair encoding;
it preserves selected optimizer, no alternate Hungarian implementation. Source
and generated vendor hashes are pinned in PROVENANCE. Final rest lifecycle test
is pending now: explicit rest must lose routine tag and queued rest append.

Current source is reviewed only as a simulation checkpoint, not final UI or
hosted behavior. Final Fallow must name actual findings; do not suppress pathname
consumers or edit upstream to improve metrics. Inherited drawSites hotspot,
HUD model complexity and shared art bundle warning remain review signals.

Root owns changed art geometry and bake. Visitor study evidence:
`.botanical/visitors-motion-corrected-1788767211/`: true8walk phases, four idle
facings, native640/narrow390, pause and image state. Root personally inspected
`all-walk-frames-review.png`, four facings and native/mobile. Five figures each
have5distinct texture images across8phases (symmetric phases), not8uniqueimages.
Earlier `.botanical/visitors-first-render-1788766278/` has valid stills but INVALID
motion claim: proof rotated while paused. Preserve it and use corrected proof.
Root will provide Sedge's task poses/bake and review those changed pixels.

## Delivery permissions and operating rules

Read `/home/levi/src/Botanical-next/.agents/skills/orchestrate-multi-lane-work/SKILL.md`,
its Field Guide delivery loop, wall-of-shame skill AND Wall before briefs; use
source-before-design and deletion test. Herdr skill is
`/home/levi/.codex/skills/herdr/SKILL.md`; verify HERDR_ENV=1, installed help owns
syntax, explicit --session botanical and absolute Hive cwd. Native waits <=55s,
completion notifications instead of transcript polling. Preserve focus/panes,
CTO+Delivery topology and working authors. Only add a useful independent visible
PM in Lanes if needed and review capacity exists; PMs report to you. Luna handles
bounded implementation, Terra review, Sol harder reasoning. As of 2026-09-08,
ordinary scoped or sustained Shiitake implementation is supported when it has a
visible owner plus retained run/report supervision. Autonomous PM recovery,
multi-worker recovery, and goal-terminal continuity remain unproven; do not treat
those capabilities as delivery dependencies. Earlier one-shot-only launch limits
are historical evidence, not current handoff instructions. This visible Sol Codex
lead remains the explicit coordination fallback. Do not build runtime glue.

For current and future Shiitake handoffs, normal exec may report tool friction
directly with `herdr --session botanical agent prompt cto "agent report: <sanitized report>"`.
Include worker identity, absolute cwd, operation, sanitized expected/actual/error,
and workaround; retain the same report in the ordinary Game Delivery result. If
that command or reporting path fails, preserve the existing saved completion
handoff and explicitly name the reporting failure. Never include environment or
credential dumps. Botanical owns diagnosis. This reporting route does not reopen
the retired `:41342` host/store or authorize replaying its work.

ALL automated browser/TUI/test/build proof launches use the existing runner:
`/home/levi/src/Botanical-next/.agents/skills/orchestrate-multi-lane-work/scripts/run-proof.sh`.
10min scope deadline,5sec grace. Retain/poll returned native exec sessions30–45s
until exit; no uncontrolled backgrounding, no stopping others' scopes by age/name.
Existing human dev5187/dist5188 servers stay. Browser paths:
CHROMIUM_PATH=/home/levi/.cache/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell
LD_LIBRARY_PATH=/home/levi/src/Botanical-next/.botanical/browser-libs/root/usr/lib/x86_64-linux-gnu
Earlier unexplained143 evidence is preserved; latest isolated traced full-home
proof of016b1a0 passed exit0 in `.botanical/controls-hosted-home-traced-1788765178/`.
That does NOT prove this dirty candidate. Stop retrying if unexplained TERM repeats.

After explicit Git transfer, you alone integrate/commit/push exact feature branch
and refresh https://goblin-mvp-fungi-goblin-bnb.levi-fe0.workers.dev/ including /study.
No main merge, production/backend, purchased resources, other Workers or repos.
Wrangler4.127.1 native static preview; config already owns fungi-goblin-bnb,
workers_dev=false, preview_urls=true. Generic npm preview script stays native.
Authenticated subprocess must use `set -a` before sourcing and `set +a` after
to export the file's assignments to npm/Wrangler. Verified September9: merely
sourcing left the token out of the child environment; the provider was not
rejecting a supplied credential. The already-authorized private environment is
`/home/levi/src/Botanical-next/.botanical/credentials/cloudflare.env`, then set
CLOUDFLARE_ACCOUNT_ID=fe06bade61f12db4c9af035423185f7f and run
`npm run preview -- --name goblin-mvp`. Never read/print/copy credential values or
commit host paths. Native git push helper previously used:
`git -c credential.helper='!mise exec github-cli@2.100.0 -- gh auth git-credential' push origin HEAD:refs/heads/feat/goblin-bed-and-breakfast-mvp`.
Verify exact remote ref, actual hosted interaction and separate artifact parity.
Send Astra meaningful playable result/revision/URL/actions for direct Levi updates.
