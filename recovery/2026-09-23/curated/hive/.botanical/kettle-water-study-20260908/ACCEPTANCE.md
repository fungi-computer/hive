# Clear water in the accepted brew kettle

2026-09-08. Astra personally viewed `render-v2/native-and-2x.png`. Accepted addition: a quiet teal water surface with two small reflected stripes, reusing the accepted pail's water palette, inside the existing hollow copper vessel. The first consumer should use the shown `appearance: "water", level: .8, moving: false` for its filled kettle; omit the contents primitive when empty. Runtime quantities and container capacity remain simulation facts, not this visual level argument.

The low `.4` diagnostic remains contained and pixel-distinct but is difficult to read at native scale. It is not the recommended initial filled-kettle appearance. Future partial-fill presentation needs its own actual consumer/readability decision; this does not silently claim a volume-accurate fluid surface. Default amber wort, empty body, paddle, hearth and all existing activity appearances remain unchanged. Plain water does not start a fire, steam, stirring or fermentation.

## Evidence and limits

First attempt `run-u2474.scope`, invocation `10e593d313c44397bdbae1acd4ad02f9`, exited 1 because the previously retained human Vite endpoint at port 5187 was absent. No art assertion ran. Its failure JSON remains in `render-v1/`; no human server was restarted.

Corrected owned `run-u2479.scope`, invocation `160355c5a7584b5ebefd8e5caa609662`, completed normally with exit 0. It used an ephemeral localhost Vite server inside that same bounded invocation and closed both server and browser normally. `render-v2/proof.json` separately reports `passed: true`, unchanged inventoried sources and `errors: []`.

- Eight composed station bakes: empty, two water levels and wort in the two actual building facings, using the current 112x112 bake/anchor and accepted centered station datum.
- Nonempty unclipped alpha; liquid above the actual inner floor and below the mouth; unchanged 2x2 footprint; no fire.
- Twenty default-art pixel-parity pairs against the previously accepted kettle: empty/staged plus all eight working phases in both production facings. This is bounded parity, not a fresh all-four-facing art suite.
- Eight physical study-body clicks, background rejection and repeat disposal.

No gameplay, numerical volume, liquid physics, recipe, save, production caller or hosted proof is claimed. The first joined caller remains Delivery's responsibility. No second original-art run/gate is required for these unchanged accepted bytes.

## Exact handoff

`art-integration.patch` changes only the shared `src/art/brew-vessel.js` contents palette/appearance input. The current production provider is a formatted relocation of the accepted source; the candidate preserves its existing imports and all other functions. Astra read the resulting narrow diff. Formatting of the ignored integration candidate completed in `run-u2487.scope`, invocation `356d46f8750d4a558620dd4a299505e2`, exit 0. The earlier formatter invocation skipped the ignored path and is not claimed as formatting evidence.

| Artifact | SHA256 |
| --- | --- |
| Rendered ignored `kettle.js` | `89872a1f6fa4e5e8a3d2e9d78d396d5ab322cd0898ec383e8ddef8b2265760b7` |
| Study caller | `895df0abbb28f3813bbb52be38e04035adf79cf5a0460217b92cb6e62c3ae630` |
| Proof harness | `490a1c855da1bdb417fd99cdcb58857a607af85b6a032683040c92147e961150` |
| Final proof JSON | `abc04c31d1f1c6de4b9effec4472b8ba1162708f7721f660a44504e6137a1a68` |
| Native/2x contact sheet | `b27cf2a12dbf6079375d0520b14abd1eb37582ef565674a1e42b7564929bff25` |
| Shared-provider base | `f9be1df1975c0f5cb4f0750448110ec0b6b2128b149e69a0eb2e42d3c34b9763` |
| Formatted integration candidate | `37101d3793954f6b52c80c815a82cd6ecbd0f77a9b3bebf65d8bc79eaefe2270` |
| Integration patch | `bd467eae7d6c475ef9bfdb646e8b2cff68e1291f6b0b3e656a5024eceedea277` |

Delivery owns serial application after its current shared-art author releases that file, then the actual runtime water-state selector. Apply the centered station transform once; water shares the same station target and visibility. The study adapter owns no material, pail, task, tick or saved state. Existing source/persistence work continues independently.

## Integrated source checkpoint

Delivery applied the exact appearance patch in `7aeb29c`, followed by `9f78aa3` connecting the centered station scene to the existing `home.js:building` entry point. Astra re-read the immediate caller and independently confirmed shared `src/art/brew-vessel.js` SHA256 `37101d3793954f6b52c80c815a82cd6ecbd0f77a9b3bebf65d8bc79eaefe2270`, exactly the accepted candidate. This is source integration, not a claim that the pail workflow or water selector is hosted.

The latest root hosted read (`u2515`, invocation `c0e929290bfe445eb38e391d143cd735`, exit 0) fetched the index with a browser-like user agent and received HTTP 200 with SHA256 `6b9e54b9f78c37922960e2d3f826e89bc560781eaa471295cfe0df4f0486ea93`, equal to the retained 54c release index. It was index-only, not a new full-asset/browser proof. An earlier plain urllib request returned 403; the subsequent successful requests do not establish a preview outage or deployment credential failure.
