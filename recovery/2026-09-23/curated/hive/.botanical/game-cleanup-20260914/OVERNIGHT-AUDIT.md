# Overnight conservative cleanup audit

Audit time: 2026-09-14 UTC

This is a read-only inventory. No worktree, branch, commit, source file,
`.botanical` directory, credential, process, listener, or cache was deleted or
changed by this audit.

## Scope and evidence

The inventory used `git worktree list --porcelain` (351 registered worktrees),
then ran `git status --porcelain`, `git rev-parse HEAD`, upstream tracking and
remote-contains checks in each worktree. Process safety was checked with `ps`,
`/proc/*/cwd`, and `ss -ltnp`.

No active Hive/Goblin Wrangler, Worker, Vite, Cargo, Rust, browser, npm, pnpm,
or test process was found. The active Codex sessions are rooted at
`/home/levi/src/hive` and `/home/levi/src/Botanical-next`; they were preserved.
The only identified named listener was an unrelated `cloudflared` process on
127.0.0.1:20247. PostgreSQL and several unnamed container listeners were also
present; no listener could be tied to a removable Hive worktree.

The status pass found 20 dirty worktrees, including the root and the active
`workattempt-consolidation` writer. It also found 315 clean worktrees containing
`.botanical`. Those are excluded from cleanup by policy even when their Git
state is clean. The active and review roots explicitly retained include:

- `clearing-repair-acceptance` (`db55482`, clean, pushed)
- `clearing-playable-prep-20260914` (`5731c9b`, clean, pushed)
- `depth-acceptance-prep-20260914` (`7519e32` ancestry, clean, pushed)
- `party-release-prep` (`0bf6e35`, clean, pushed)
- `workattempt-consolidation` (`c3bb096`, dirty, active writer)

The first four contain `.botanical` evidence and remain preserved. The last is
dirty and has the active Rust target; it remains preserved regardless of size.

## Possible later removals

These are the only registered worktree directories that met the narrow source
test of being clean, detached, lacking `.botanical`, and having their HEAD
reachable from a remote branch. They are duplicate release snapshots rather
than active branches. They were not removed because this audit was read-only;
an owner can release them after confirming that no external review still points
at the path.

| path | HEAD | size | evidence |
| --- | --- | ---: | --- |
| `/home/levi/src/hive-brew-release-20260908` | `1d90cd52a3a3f1be1eabc7d166f25d00188ead16` | 9.5M | detached, clean, no `.botanical`; commit is contained by multiple `origin/*` branches including current clearing branches |
| `/tmp/hive-brew-art-release.NEicej` | `e53736424525567f5cbce5261e2937d86ddfe2e5` | 9.1M | detached, clean, no `.botanical`; commit is contained by multiple `origin/*` branches |
| `/tmp/hive-dig-release-dimRCH` | `449e9b8a642dc1c9e6d815f8e7417c143ba1e574` | 18M | detached, clean, no `.botanical`; commit is contained by multiple `origin/*` branches |

`/home/levi/src/hive-study-release-20260908` is clean and detached (9.3M),
but its `e1c9c6b` commit is not contained by any remote branch found in this
checkout. It is excluded from the possible-removal list and must be retained
until its provenance is resolved.

No tracked branch worktree qualified as a deletion candidate: every clean
tracked branch either has `.botanical`, has no upstream/remote proof, or is an
active/review lane. The two clean no-`.botanical` tracked side worktrees with
upstreams (`hive-delivery-rejection-fix` and `brewing-water-join`) are 120 and
148 commits ahead of their upstream respectively and are explicitly excluded.

## Reproducible non-`.botanical` cache candidates

These are generated directories only, not deletion actions. They can be
removed later after checking that their owning worktree is inactive and that no
current proof/release references the generated bytes:

- `/home/levi/src/hive-worktrees/workattempt-consolidation/engine/kernel/target`
  — about 2,250,920 KiB (roughly 2.15 GiB). **Excluded for now:** the owning
  worktree is dirty and actively being edited.
- `/home/levi/src/hive-worktrees/authored-orders/dist` — about 31,272 KiB.
- `/home/levi/src/hive-worktrees/fresh-engine/dist` — about 30,820 KiB.
- `/home/levi/src/hive-worktrees/colony-performance-playground/dist` — about
  20,360 KiB.
- `/home/levi/src/hive-worktrees/goblin-environment/dist` — about 13,968 KiB.
- `/home/levi/src/hive-worktrees/wet-release/dist` — about 10,040 KiB.
- `/home/levi/src/hive-worktrees/gameplay-water-release/dist` — about 8,992 KiB.
- `/tmp/hive-dig-release-dimRCH/dist` — about 8,472 KiB; removable with its
  detached duplicate worktree, subject to owner confirmation.
- `/home/levi/src/hive-brew-release-20260908/dist` — about 4,728 KiB; same
  detached-release condition.
- `/home/levi/src/hive-study-release-20260908/dist` — about 4,684 KiB; retain
  with its unresolved detached study snapshot.
- `/tmp/hive-brew-art-release.NEicej/dist` — about 4,628 KiB; removable with
  its detached duplicate worktree, subject to owner confirmation.

The scan also found tiny generated `dist`/`node_modules` directories under
temporary `/tmp` audit fixtures. They are not game worktrees and were not
selected for deletion because their owning temporary fixture and provenance
were not part of this game cleanup scope.

## Explicit exclusions

- Every dirty worktree, including the root, the active WorkAttempt writer, and
  all dirty game/editor/proof lanes.
- Every worktree containing `.botanical`, including clean acceptance, party,
  depth, playable, toolchain, proof, and recovery lanes.
- Every branch and commit, including unpushed branches and commits reachable
  only from local refs.
- Credentials, proof receipts, saved bytes, deployment artifacts, accepted
  source, and current workattempt/playable/depth/party roots.
- All `target`, `node_modules`, and generated output tied to an active or dirty
  worktree. The prior cleanup report records caches already removed; this audit
  performed no second removal.

Recommended next cleanup action is owner-confirmed removal of the three
detached duplicate release worktrees and their `dist` directories, followed by
an independent check of the unresolved `hive-study-release-20260908` snapshot.
