# Fire study proof v1

Run from `/home/levi/src/hive` with the existing bounded proof runner:

```sh
bash /home/levi/src/Botanical-next/.agents/skills/orchestrate-multi-lane-work/scripts/run-proof.sh \
  node .botanical/fire-study/prove.mjs .botanical/fire-study/proof-v1
```

The WebM is a five-second court-canvas motion capture after startup, with
timed programmatic palette controls. Playwright palette and pause interactions
are asserted separately in the proof.

Final bounded run: systemd scope `run-u627.scope`, native exec session `21207`,
normal exit `0`.
