# Frozen finite low-Mach spatial checkpoint

Accepted numerical checkpoint: u2995 / `b29b42f1ef7f45c8b3f4ee77e537ecbd`, exit 0, 7 groups / 87 checks, 0.842308 s measured wall time. Fallow u3000 / `343fb5d1a52647b7bc509ceae56c1dd9`, exit 0; actual health advisories are disposed in `RESULTS.md`. Independent acceptance is in `REVIEW.md`. Source and complete evidence hashes are in `source-inventory.json` and `handoff-inventory.json` respectively.

The public finite physical identity remains `sealed-single-species-expansion-ssprk2-v1`. `createFiniteGas` exposes initial/read/advance/forwardEuler/encode/decode plus diagnostics. Canonical stocks are cell mass and internal energy, with checked clock and initial/source balance references. Geometry and definition identity are bound; pressure/temperature/density are derived. The shared `project` uses a target integrated volume flux and positive face mobility, with per-component compatibility, per-stage numeric rebuild and all-cell residual including the gauge cell. In the finite caller phi is pressure impulse (Pa·s), not instantaneous pressure.

Files to read together: `CONTRACT.md`, `finite-gas.mjs`, `projection.mjs`, `geometry-owner.mjs`, immediate `boussinesq-reference.mjs` consumer, independent `../LOW-MACH-HEATING-ORACLE.md`, and `qualify.mjs`. `qualification-v1.json` is the outcome, `strip-dt{4,2,1}.json` retains full state/face receipts and independent cell-average profile, and `restart-state.json` is the actual saved intermediate state.

No tracked files, game/runtime callers, renderers, server, dependency, Git or deployment changes are part of this handoff. Existing frozen Boussinesq, finite thermodynamics, voxel bindings and visual datasets remain unchanged. This directory's numerical bytes and first proof are now frozen. The only authorized successor is the separate `../finite-low-mach-refactor-v1/` behavior-neutral decomposition, preserving physical identity, arithmetic/reduction order, limits and rejection behavior and proving complete state/receipt equivalence against this checkpoint. No new physics is implied by that refactor.
