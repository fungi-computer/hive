# Preserved compile checkpoint; production fidelity reset

Root relayed Levi's fidelity correction after the already-running compile:
stop the conditional new 100 J / 2 s physical packet and prioritize
voxel-scale believable, conservative behavior with bounded real-time cost.
No numerical process was interrupted. No new regression or physical run
was started. This note supersedes the historical authorization/status
paragraph in the pinned CONTRACT; those exact compiled input bytes remain
unchanged.

## Actual terminal

Owned compile scope run-u3530.scope, invocation
1f2e7876b4be4322a0c2a29e9cb0e2ee, session 57361, completed with exit 0.
Three serial commands compiled the captured-system regression (29.005 s),
generated the physical caller (2.981 s), and compiled the physical binary
(28.596 s). Total elapsed 60.582 s. These are compile times, not simulation
costs. The session is terminal; the numerical window was released to root
and water_solver_round3.

The regression executable exists but has not been run. The proposed
captured-system bitwise comparison, one-correction result and rejection
cases remain unproved in this compiled candidate. No refined physical
result, completed heater cutoff, 100 J release or 2 s endpoint is claimed.

## Source/generated review

- Every EXECUTABLE-CHECKPOINT source and upstream hash still matches.
- sealed-heat.c, thermal-overlay.h and sealed-heat-observation.h are
  byte-identical to the frozen preceding coarse candidate.
- All 34 generated event registrations equal the preceding generated
  caller after normalizing only source filename/line provenance.
- Generated code contains one original-stencil signed residual owner,
  one optional correction branch, and one shared inverse-product owner.
  All 16 correction components derive from the same saved residual.
  The local solution is updated only after every corrected value is
  finite; native fields are assigned only after final guard acceptance.
- The native helper remains restricted to level 1; finer levels use the
  original thermal relaxation. Generated probeT/probep scratch is deleted
  at the end of the boundary check.
- Passive failure output consumes shared pre/post residual records. It
  does not supply another physical update or acceptance stencil.

These are source/compiler findings, not a numerical or physical pass.

## Compiler limitations retained

Normal C diagnostics were not empty: command-1.log contains eight and
command-3.log contains four instances of GCC's pre-C2X pedantic warning
about pointers to multidimensional arrays with different qualifiers.
They occur at const-qualified matrix helper/capture call boundaries.
There were no C compile errors. The exact warnings are preserved; this
checkpoint is not described as warning-clean. No compiler-only rewrite
or second build was started after the fidelity reset.

The previously authorized local -disable-dimensions option was used for
the heterogeneous T/p dense algebra. Physical units were source-reviewed;
qcc dimensional analysis is not claimed to have passed. Installed qcc
and upstream headers remain unchanged. Fallow does not qualify these C
owners; no JavaScript reachability claim is substituted for C review.

## Useful retained conclusion

The prior coarse solve reduced native multigrid cycles over the same
completed 25-step prefix from 1758 to 124, while aggregate physical
observables closely matched. It then rejected a coarse backward-error
guard at native t=0.24735390459260645 s. Exact captured-system analysis
attributed that failure to inverse/dot-product roundoff, and one ordinary
residual refinement passed the unchanged guard offline. This new source
implements that specific standard correction, but its native regression
and physical outcome remain unmeasured.

The retained lesson is that coarse pressure/thermal solve conditioning
can dominate work. It is not evidence that this reference solver should
be the production voxel game model. No accuracy/tolerance sweep, native
adoption, gravity, openings, moving terrain or full gas/water coupling is
authorized or claimed by this checkpoint.
