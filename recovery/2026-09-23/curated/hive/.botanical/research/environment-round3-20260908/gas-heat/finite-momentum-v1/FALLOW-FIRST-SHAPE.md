# First source audit, before dynamics

All numerical source remains at the first-source-checkpoint pins. No momentum dynamics qualification has run. The current caller is `qualify.mjs` SHA256 `2b49c335064feeb763d8cad67b7445c2ae4311623d1f1f9c065a1d3f9ec766bf`.

Syntax-only u3138 / `eda6d28054af47fc8a0f5906241bb1ad` exited0 for the five source/caller modules. A later reporting-only caller addition records perturbation-pressure ratios and checks the existing RK weights; the focused current-caller inspection below includes it. Combined Fallow3.20.0 u3140 / `a1067daf1e184061937be6073716df9b` exited0 and retained `fallow-first-shape.json` SHA256 `c4374d2747cd31a503335424450b6b4225b4328025b12b82a070c50351d5eccf`. It reported25 health findings and four unused public exports. Exit0 is not an all-green audit claim. The missing local node_modules warning is retained; no dependencies were installed.

Focused inspections on the actual current source and caller all exited0: u3142 / `db8ad3d7e4574bd181e3bf20db370be5` for finite-gas; u3143 / `441b0fc266cc40e6af96a0068fd23ad9` for momentum; u3144 / `5d450378aa7f4432aef199d03eaae9ff` for qualify. Their JSON outputs are `inspect-*-first.json`. Each command used the ordinary shared-host proof guard and was retained through terminal completion.

The largest cognitive finding is in the proof's `basisCase` (CC17/cognitive32), which currently joins independent geometry bounds/ownership checks and every-basis linear-map verification. That is a real readable-boundary candidate: those responsibilities can be separated without sharing the solver's flux construction. The current equations and predetermined qualification remain frozen for parent caller review; no threshold is suppressed or test removed to hide it.

Numerical source advisories include finite `validate` (CC19/cognitive13), `momentumGeometry` (10/15), `transportMomentum` (10/10), `dualBounds` (7/10), and `acceptTrial` (6/9). Validation now owns shape/identity, thermodynamic balances, momentum impulses and committed velocity constraints; separating those admissions is a credible later behavior-neutral improvement after the first physical result is frozen. The geometry constructor already separates cells, primal lookup, bounds and nodes, but interface connectivity plus unique active ownership remain in one25-line operation. Momentum transport's26 lines directly sequence mass consistency, paired transport, forcing, wall reaction and operation receipts. None of these advisories certifies correctness, and the lack of a runtime coverage artifact makes their CRAP scores estimated rather than measured coverage.

The shared pressure extraction retains its previous responsibility split: target admission cognitive11, PCG10, numeric assembly6, correction7. The finite Euler coordinator is cognitive3; retry10 and accepted-step accumulation9. The factory's238-line extent includes its private methods, not238 sequential statements in one update. Source ownership remains one thermal stock/clock, one derived momentum partition and one pressure projection.

VERSION, LIMITS, MOMENTUM_LIMITS and PROJECTION_LIMITS are useful public method/limit metadata and internal constants. They remain exported even though this one declared proof entry does not import them. There are no unused files, unresolved imports, cycles or clone groups inside the five audited files. Frozen upstream numerical references are intentionally outside this narrow audit root; their retained duplication has not been erased. Focused inspections show no security findings, but their dynamic-call limitations remain and are not a general security certification.

The caller has independently derived geometry/basis checks, continuous root-owned momentum and pressure references, local mass/U/P receipt reconstruction, actual-file continuation, stale provider/domain/method rejection and copied-pressure checks. These are source-reviewed intentions until the authorized single numerical run produces evidence.
