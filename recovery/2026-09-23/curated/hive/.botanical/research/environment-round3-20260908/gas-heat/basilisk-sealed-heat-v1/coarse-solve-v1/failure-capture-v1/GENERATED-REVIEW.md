# Capture build and caller review

u3478 / a71894aa7c124f659af20c0bd1ca3d26 compiled successfully in
31.804 s (source translation3.052 s, binary28.751 s), empty C diagnostic
logs, upstream and source pins unchanged. The inherited isolated qcc
dimensional-analysis bypass remains explicitly unqualified.

The physical caller, stock observer and local thermal overlay are
byte-identical to the frozen failed candidate. All34 generated event
registrations match after source-path/line normalization. The actual
failure branch calls capture_rejection at generated line16946, immediately
before the same coarse_reject at16947. The original acceptance expression
and physical commit positions are unchanged.

Generated copies occur before matrix scaling, after row-scale selection,
before inversion, after returned inverse/pivot and after the actual
solution dot product. They do not replace numeric operands. All capture
output is reached only after the guard has already rejected. No time,
heater or finite quantity is changed by the recorder.

One reproduction under the original caps is now permitted by root's
diagnostic assignment. It remains a rejected physical packet even if the
new diagnostic capture succeeds.
