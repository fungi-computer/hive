# Source and caller checkpoint

The owned caller is `qualify.mjs` SHA256 `0f043edb0e023b6d2f53e27864231dcbbece05f18d6a3b0b053f9d56a0ffbda4`; fixed contract `fa9551f512d1eb3c8321481b08d4d254c95dc1af667cc9bf93ef01ac2b14dc53`. No numerical case has run at this checkpoint. The caller imports the unchanged accepted finite-momentum owner and root's independently checked buoyancy reference, whose hashes and prior handoff inventory are explicitly verified before/after the packet.

Parent's first-source correction was evidence retention: a returned physical failure must preserve the measured field before an assertion interrupts later output. Each returned exposed Euler stage, short evolution, finite moving run and restart segment now writes a `case-*.json` snapshot immediately. Moving values and local laws are persisted before circulation thresholds; the failure report includes the current active case. This changes output ordering only. The physical model, fixture, thresholds, timestep, source input and numerical owner did not change.

Syntax-only u3204 / `b83b407399da4ad4bc46a55b416d5892` exited0. Combined Fallow3.20.0 u3205 / `8f3e2fa6850b45d3a7df17dabac84229` exited0 and retained `fallow-first-shape.json` SHA256 `995324fa8ebda81fb7e53bc461241fff8c84bf5aef96e13015792a8d8e8dc37a`. Both used the ordinary proof guard; the retained Fallow session61854 reached terminal completion. Its missing local node_modules warning remains; no installation was attempted.

The declared entry is the actual `qualify.mjs`, not a fabricated runtime root. Its three health advisories are estimated-without-runtime-coverage CRAP: `runMoving` CC7/cognitive3, `reconstructPhysical`5/4 and `movement`5/9. None exceeds the cognitive threshold. These are real admission/reconstruction/measurement branches, kept explicit and read alongside the frozen quantity/impulse source. No suppression or deletion quieted the audit. There are no dead-code, unresolved-import, cycle or clone findings **inside this one owned caller file**. Frozen upstream modules are outside this narrow audit root; their prior reported advisories are preserved, not claimed resolved.

The caller's expected acceleration and perturbational pressure come from continuous analytic averages; the gas receives only its actual fixed gravity and finite initial density. Stock/impulse/PE checks reconstruct from independent paired receipts. The exposed initial Euler response is reported separately from returned accepted SSPRK steps. Static/source evidence does not yet establish the moving physical outcome.
