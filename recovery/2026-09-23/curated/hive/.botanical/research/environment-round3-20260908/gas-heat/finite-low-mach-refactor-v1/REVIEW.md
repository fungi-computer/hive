# Independent internal-refactor review

2026-09-08. **First source shape accepted for its bounded equivalence proof.** No new physical qualification is claimed by this decomposition. The reviewer read complete diffs of `projection.mjs` and `finite-gas.mjs` against the accepted sibling, confirmed unchanged geometry/Boussinesq files, and read the original actual numerical/Fallow records and frozen handoff inventories. No reviewer solver edit or numerical run occurred.

The frozen reference remains `../finite-low-mach-v1/`. Its qualification has seven groups/87 checks; every recorded source pin and handoff hash matches its actual file. Its numerical result and limits are independently accepted in that sibling's REVIEW. The real Fallow hotspots justified this change: projection cognitive45/cyclomatic34, finite advance26/20 and projection workspace17/12. This is not deletion of compatibility exports or an attempt to relabel estimated CRAP as executed coverage.

## Exact source comparison

The projection split gives names to existing responsibilities: face connectivity, one-component traversal, complete component/pin admission, input shape, sealed target compatibility, numeric coefficients, initial residual, PCG and final corrected-flux validation. The depth-first traversal retains the same seed order, adjacency order, stack operations and visited semantics. Component freezing moves just before the pin assertion; a rejected local allocation is never exposed or cached. No accepted numerical geometry changes.

Coefficient rebuilding still occurs after compatibility and before every solve. The matrix product, dot products, residual updates, diagonal pinning and face velocity correction retain their exact loop order and arithmetic. Linear residual is captured before divergence scratch is reused. All-cell constraint validation still includes the gauge cell. Counters advance at the same numerical boundaries; the shared projection remains the single owner used by both finite and Boussinesq consumers.

The finite split separates advance request admission, bounded two-stage trial, accepted accumulator initialization, physical acceptance and public result construction. The initial field read occurs at the same point after admission. A trial still performs the same Euler calls with the same named-envelope/CFL reductions. The average mass/U/source arrays and weighted face/source receipts retain the same arithmetic order. The second stage's receipt remains the last-stage diagnostic.

Rejected counts and reason counts now accumulate into the accepted local result after a successful trial, rather than updating local counters during each failed attempt. These are bounded integers with identical final values. When a complete request throws, neither implementation exposes that local accumulator; physical input stocks remain unchanged. Existing numerical diagnostic attempts still count the actual stage calls. This is not a new retry policy or partial physical commit.

The public physical VERSION, full identity, limits and existing finite thermodynamic owner are retained. No compatibility save migration is needed for reorganizing identical arithmetic; requiring one would falsely identify source layout as a different physical recipe. The unchanged private geometry owner and immediate Boussinesq caller are byte-identical, and their relative imports still point to the same retained dependencies.

No source blocker was found. The separate equivalence caller and its result remain to be reviewed before final acceptance. Required evidence is complete canonical state/receipt/diagnostic equality where promised, unchanged meaningful rejection outcomes, and same-identity restore across the old/new owners, together with honest Fallow disposition. This review does not authorize broader physics or a second proof suite.

## Equivalence caller checkpoint

The completed `qualify.mjs` and `REFACTOR.md` have now been read before execution. The caller retains the seven physical groups, compares full dt4/2/1 canonical states/aggregate receipts/exact oracle masses/work against saved reference evidence, and compares complete actively computed old/new output objects and diagnostics. Actual CFL reduction and the intermediate-envelope reduction are exercised, not merely zero-retry cases. Both existing inert definitions are included without widening the model to mixtures.

The codec test restores one owner's actual encoded finite state in the other and checks subsequent complete outputs. Meaningful rejected intervals, source data, identity/shape, envelope and exhausted accepted-step budget compare error class/message and unchanged caller input; diagnostic counters compare too. Shared projection errors and actual Boussinesq advance/direct transport are compared through their real public callers. The reference handoff inventory is checked before and after those read-only calls. No old proof script is imported, and numerical scratch mutations in the old runtime do not write reference evidence.

This is a well-bounded regression packet, not exhaustive equivalence over every possible input. It complements the exact source diff read and is accepted for the single owned qualification. Numerical source hashes at this checkpoint: projection `6544aaef2b5fbe6ab667a0b199bb7f385c47603dfd4f6683baa02aa44ee17d09`, finite gas `ae911a681b783965fc15df8c8c47b7f987972b50a1a128049179885c5a7eecf4`. Unchanged geometry owner is `0b63271b7c2b8961f75f5e843a1919b60c94b690590d5f3eae7be98d6fac2f77`; unchanged Boussinesq caller is `fe464e909b134ea87d6e76688585a862d35e593bc7c23a6b70ba92b871fb753a`.

## Final evidence acceptance

**Accepted as the behavior-preserving successor to the frozen finite gas reference.** I read the actual `qualification-v1.json`, `source-inventory.json` and Fallow output, checked every proof source pin against the corresponding file, and independently rechecked the complete original handoff inventory. No mismatch was found. The reviewed numerical hashes above are unchanged; the complete qualification caller is `c9ce63703e29795f28e3916b5d795c5696ab05f78b055c38ae9421828ff74b63`.

The owner reports scope `run-u3023.scope`, invocation `51bbb41f383e44a292992c740e9dd4e8`, exit0. The actual record has10 passed groups/211 checks,2.877545 s measured in-process wall time and1.708377 CPU seconds. Its SHA256 is `b72936bacc7fa4ffa0f840f917ad66c0b4b6fce5b7458dc8fd0f810c6f370d30`; source inventory SHA256 is `c84fd4963627116038a418dd51de4b0b4f23195b7c62a878d81818ff56553253`.

Complete old/new output and work equality passed for rest, zero interval, local Euler, the heated strip, actual CFL reductions, intermediate-envelope reductions and the existing helium definition path. Retained dt4/2/1 states, aggregate receipts, independent oracle masses and numerical work match the original evidence exactly. Same-identity cross-owner restore passed. Nine meaningful finite-admission failures retained error class/message, physical-input bytes and diagnostic counts. Shared projection and actual Boussinesq advance/direct transport also compare completely. All original physical groups still pass; the old frozen reference inventory remains exact before and after the run.

Actual Fallow SHA256 is `7b726b8a6aca7498cc2a764eb201f224f9841f3f93612f4e4941f471cef65f99`; owner reports `run-u3025.scope`, invocation `c54090536e1b43a883cebda97f28c206`, exit0. Its narrow five-file analysis has no unused files, unresolved imports, cycles or detected clones. Ten public-export advisories remain, with no removed compatibility exports. The two finite version/limit exports are now exercised by the equivalence caller, accounting for the change from12 rather than a source deletion.

The large joined projection/advance/workspace hotspots are gone. Separated numerical functions have cognitive scores at most11 in the reported findings: target admission11, PCG10, bounded RK trial10; the two remaining above-threshold cognitive findings are unchanged Boussinesq advance21 and predict16. Total health findings rose from13 to19 because newly separated functions receive their own estimated-no-coverage CRAP entries. That count is disclosed; it does not mean the source is entirely clean, nor negate the actual reduction in joined responsibilities. No imported runtime-coverage claim is made.

The original restricted physics, first-order spatial transport, floating residuals, same-schedule restart scope and absence of full3D momentum/topology editing remain unchanged. The longer proof timing includes added equivalence work and shared-host variation; it is not a before/after runtime benchmark. No production integration, extra physics or reviewer numerical rerun occurred.
