# One wet soil voxel becomes an ordinary vented excavation

Source checkpoint, 2026-09-09. No execution or physical result yet. This directory alone is owned by the soil author. The accepted connected-volume, moving-block and world packets remain unchanged.

## Existing owners and this boundary

`volume-with-pit-v1` is a separately pinned extension of the accepted `../volume-v1` soil owner. Retention is still imported from the unchanged accepted `../column-v1/source-v2/soil.mjs`. The dense Newton direction, mixed backward-Euler mass equation and bounded step policy remain the existing mechanism. Only typed pit geometry, integrated side faces, boundary admission/initial guesses and closure verification are extended. All former reservoir inputs retain their original descriptor/solver identity and arithmetic path. `../../worldgen/coordinate-hash-v1/voxel-world.mjs` remains the actual generated-material, sparse-edit and world-checkpoint owner. No noise sample, material label, pressure guess or cache creates water.

An initial checkpoint explicitly supplies the existing world checkpoint, soil geometry and canonical soil state. Every porous cell must actually read as soil in that world. The soil geometry revision must equal the world revision. The adapter owns no caller-supplied mutable world instance: it restores a private world when admitting an operation, stages its actual revision-checked edit, then returns a new checkpoint only after every soil and combined-ledger check succeeds. Failure leaves the entire input unchanged.

The first operation removes exactly one wet soil voxel. It requires a fresh expected world revision, an owned target, an unobstructed vertical air route to the finite world's upper boundary, and ownership of every adjacent soil voxel. It rejects an existing reservoir port on the target, an already closed soil face at the target, unsupported disconnected post-edit soil, an exhausted revision, a second excavation, and backfill. The source voxel's existing pore water is moved, unchanged, to one named finite wet-spoil export. That export records source bulk volume, not a fictitious dry-solid mass or loose-spoil volume. The ordinary unlined pit begins with exactly zero free water in its actual solver reservoir node. Its available volume is .54 m³; no reference air/oxygen stock is added.

Remaining soil/reservoir water is copied by stable node ID through the existing owner's public initializer and decoder, retaining the accepted clock/step counters. Geometry and its derived topology are rebuilt. The new smaller region has its own conserved subtotal; the combined checkpoint retains the old total and includes the named export. All conservation claims are measured floating residuals using the unchanged owner's balance tolerance. Pressure is not saved; a later admitted advance would rebuild it deterministically from canonical stocks.

## Root-selected shared pit boundary

The old reservoir requires equal-height ports and cannot admit a dry multi-port reservoir. An ordinary unlined pit has floor and side faces at different elevations, partly wet side faces, and dry seepage faces. The retained `BOUNDARY.md` records that conflict. Root selected `../pit-face-v1/DECISION.md` and its `vertical-face.mjs` implementation; the candidate imports that exact primitive instead of copying its equations.

Every actual adjacent soil side is bound, as is exactly one porous floor. Actual stone sides are impermeable; an open lateral neighbor rejects until free-water spill routing exists. Vertical side rates integrate their submerged and exposed portions; empty exposed sides cannot withdraw from air. The existing one-floor negative pressure multiplier can balance side seepage with floor uptake without inventing negative water. The one physical mass is rho*A*max(psi,0); depth is bounded by the .54m rim. Pressure, face coefficients and active branches remain derived, never saved water. A saturated side of an empty pit supplies an actual atmospheric seepage reference for otherwise unanchored saturated-state admission and a deterministic trial seed, not a permanent arbitrary pressure value. A dry floor-only saturated graph still rejects if there is no other physical reference.

All ordinary closure checks remain strict. A corrected side receipt that would withdraw from an empty pit rejects instead of being clipped or accepted as tiny atmospheric water. Rim overfilling rejects the step/request until a real overflow outlet exists. This is vented quasi-static seepage/storage, not general free-water momentum, fast falling films, finite gas displacement, soil erosion or excavation of arbitrary cave roofs.

## Save, replay, bounds and first caller

Save includes world recipe/edits/revision, soil descriptor/definition/solver identity and canonical stocks, original combined water total, the one pit binding, the one named wet-spoil export and the accepted command receipt. The pit binding does not duplicate its solver mass. Derived contact geometry, resident bricks, pressure, faces and other caches are rebuilt. One historical prior target override lets decode reconstruct and replay the exact world edit; the prior geometry identity and exported source-pore bounds are rechecked. The prior live water distribution is not fabricated from later moving stocks. Exact same-command replay returns the current existing result even after subsequent flow; reuse of its operation ID for a different command rejects.

Limits stay small: one pit/export, <=64 initial porous cells, the existing <=72 pressure unknowns, <=4096 world overrides, <=1 MiB encoded adapter input, and the actual signed voxel bounds. World material reads are bounded by the region cells, six contacts and one finite upward air column, with the existing eight-brick resident cap. These are admission limits, not a performance claim. No numerical kernel is invoked in the transaction proof.

The first fixed caller uses the actual default seeded world at the nine columns x=-1..1,z=127..129 and their top two generated soil voxels. It requires those18 cells to be ordinary connected soil and the central target to have four soil sides; it will not scan for a friendlier scene, author replacement soil, or search seeds. Initial water is explicitly defined by the accepted synthetic retention law at hydrostatic total head .15m above the future pit floor, not by terrain moisture. Excavate the centre upper voxel once; preserve exact retained stocks, spoil and initially zero pit water. One fixed600s request/dt6 will require actual side and floor seepage and finite pit filling, the same conservation laws, and an exact moving300s file restart. A separate source-admission case uses a sole unsaturated target at h=-.5m with all other cells saturated, proving that removal now admits the real dry atmospheric side reference rather than imposing a saved head. Old column/boundary behavior, signed face/Jacobian assembly, zero-water no-withdraw and rim/rejection laws need source/caller checks before the one packet. No numerical run is authorized until root reads the complete caller and fixes its exact thresholds/budgets.

## Fixed first qualification packet, declared before execution

- Preserve sources before groups and preserve the first failure/partial diagnostics; one caller invocation under the ordinary wrapper and30s inner guard. No adaptive terrain/seed choice, tolerance change, extra scene or automatic rerun.
- Original four-cell moving-column consumer: saturated initial soil, finite1kg top/dry bottom,10s/dt1. Compare complete old/new state, receipts and work exactly; this is the actual retained reservoir consumer.
- Real generated excavation:18 initial soil cells,17 retained soil plus one pit. Verify each retained mass exactly by ID, source water exactly in spoil, zero initial pit mass and unchanged time. Actual floor area1m²; four side areas.54m². Combined balance uses original `2e-11 +64*EPS*abs(total)`kg.
- Full assembled mixed-residual Jacobian:18×18 bounded centred differences at epsilon1e-6m, away from h=0 and side branch kinks; tolerance`2e-6 +3e-8*abs(analytic)`kg/m. Check both left/right face orientations, negative-floor independence of side suction, and hydrostatic face flux <=1e-15m³/s. The primitive's separate root quadrature evidence is not rerun.
- Saturated-after-removal branch: the same generated fixture with only its target unsaturated at h=-.5m, all other original cells saturated. The new dry-pit atmospheric reference must admit and actually advance1s/dt1 to positive finite pit stock. No saved pressure is added.
- Primary filling discriminator:600s/dt6, with final pit stock>.001kg and<540kg; cumulative signed inflow from every side and the floor each>1e-6kg. Require real moving300s checkpoint with last-step face transfer>1e-7kg, actual file reload, and exact full canonical endpoint after the remaining300s/dt6. These are fixed outcome minima, not predictions of observed magnitudes.
- Existing strict pore/rim, mixed/constitutive/Darcy/tree2e-9kg, compensated paired/total, unchanged Darcy chord and pressure complementarity laws remain mandatory. No empty-side reverse receipt is accepted or clipped. Work remains <=72unknowns,512steps,250k evaluations and30M trailing matrix updates per request. Count actual matrices/iterations/face evaluations/rejections separately from process timing and memory.
- Reject stale/buried/second edits, unsupported backfill, corrupt export, hard matrix budget and an over-rim canonical pit stock. The latter is an admission check, not a claim that an actual overflowing trajectory or displaced-water backfill has been simulated.
