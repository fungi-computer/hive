# Point-query excavation consumer: unchanged physical outcome

One ignored next candidate, 2026-09-09. The three local modules are a pinned
development revision of the accepted excavation adapter, binding and fixed
fixture. Their only behavioral change is using the exact reviewed root world
owner's `readPoint` for sparse material validation: regional cells, old-target
history, target admission, six contacts, vertical vent and fixture soil cells.
Relative imports change only to reach the original unchanged soil/geometry
owners and the one new world owner. No alternative sampler, physics, dependency
injection framework, cache, capacity, receipt or save policy is introduced.

The earlier adapter, world and all 33 accepted source pins stay frozen. Before
execution the caller pins both those sources and these candidate modules and
verifies the precise reviewed world hash. Source copies and first-failure
progress are preserved before/through the numerical packet.

One fixed packet, <=30 seconds under the ordinary proof wrapper:

1. Build exactly the original generated fixture and compare complete initial
   state to `../run-v1/before.json`. Measure public adapter initial and excavate
   separately; compare complete cut state to accepted `excavated.json` and
   preserve input. No seed search or material/water initialization changes.
2. Advance the actual candidate adapter 600 seconds at dtMaxS=6 once. Its full
   canonical state, receipts, work and balance must be serialized-value equal
   to the accepted `moving-result.json`, not merely near in total mass. Original
   512-step/30M-update/strict-stock bounds and all physical coefficients remain.
3. Measure read, encode, fresh-owner creation and actual file decode separately.
   The accepted state and full read facts must agree. Persist a second actual
   file using the accepted moving 300-second checkpoint, freshly decode it and
   advance another 300 seconds; compare complete result to accepted resumed
   output and endpoint to the 600-second path. This verifies moving restart
   without repeating the earlier five physical groups or an extra 0-to-300 run.
4. Measure a fresh public sparse region/vent/contact pass and report real point,
   height/cave, brick and memory counters. Its contacts match actual read facts;
   zero generated bricks is required. All accepted/candidate pins are checked
   again after the packet. Exact command replay and stale-edit input preservation
   remain narrowly checked through the actual new adapter.

One observation per phase in fixed order, no warmup, repeated suite or forced
GC. Record wall and CPU separately. Previous cost-v1 is a historical phase
observation, not a paired speed benchmark. End-process RSS includes reference
and result objects, not peak solver allocation. No CPU profiler is warranted
unless these ordinary costs expose a concrete unresolved bottleneck. No
larger-region capacity, excavation gameplay, finite gas or free-surface claim.
