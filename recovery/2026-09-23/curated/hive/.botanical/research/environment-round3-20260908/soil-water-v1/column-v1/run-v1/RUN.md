# First frozen soil packet — failed, preserved

2026-09-08. Scope `run-u3172.scope`, invocation `8898b98d7a7a43daa267c116a04ef709`, ordinary `run-proof.sh timeout -k 5s 30s node qualify.mjs run-v1`, exit1. Recorded caller elapsed56.434317ms. No prior physical run and no replacement run at this checkpoint.

`source-inventory.json` and `sources/` were written before the first group. `failure.json` contains completed groups, the two completed reservoir cases, first failing group, original error/stack, and explicitly uncommitted partial state/receipts/work. The request's input was never mutated. No result is accepted merely because some internal steps completed.

Passed:49 constitutive/analytic derivative/full Jacobian/adjacent-pivot checks, then132 hydrostatic water-table and ponded-rest checks. Downward finite-reservoir cases dt60 anddt30 completed. The dt15 case stopped at the attempted390->405s step with `finite boundary complementarity inequality`;253 checks had been reached. Upward, wet/dry transitions, nonlinear exhaustion/reference and restart groups were not reached and are not claimed.

The failing request's prior accepted stage had mass-balance residual0kg, constitutive0kg, Darcy residual8.881784197001252e-15kg, paired reconstruction residual2.2737367544323206e-13kg. Work through failure:81 residual evaluations,729 face evaluations,27 Newton iterations,27 line-search trials,0 timestep retries. The failing trial's wet head/depth pair was not included in its exception; that evidence limitation is explicit.

Source diagnosis is the wet-head roundtrip: the solver keeps h, writes M=rho*A*h, then a strict complementarity inequality compares M/(rho*A) against the old h. Floating multiplication/division can make the difference negative by an ulp. This is a source-grounded hypothesis, not an observed magnitude for the missing failing pair. The proposed correction is to derive every wet boundary's final head from its final canonical water mass, then recheck all original storage/Darcy/ledger/complementarity tolerances. It does not add water, clip pore contents or relax capacity. No source correction or rerun is included in this frozen packet.

Pre-run syntax u3157 (`b01efe2dbe9342359c4be52061e92c36`) exit0. Raw Fallow u3158 (`109adf74dc7942d0890925277afe59d6`) had no declared entry and reported all7 files dead; retained as `../fallow-first.json`. Corrected audit u3166 (`8f4765deaf9e497f9ec024ad7b95e011`) declared the real qualify entry, exit0, retained `../fallow.json`:0 dead-code/cycle issues,0 clone groups,18 health advisories. Real cognitive hotspots are guessHeads32, independent explicit reference29, Jacobian fixture24. CRAP uses static estimated coverage, not measured test coverage. No functions were deleted or suppressed to turn that audit green.
