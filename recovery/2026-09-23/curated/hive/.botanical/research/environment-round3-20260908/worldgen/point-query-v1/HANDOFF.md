# Sparse-query candidate and actual first check

Root owns the frozen candidate world module; the soil reviewer owns only its
separate next adapter consumer. No tracked game/world or existing soil source
changed. Candidate world SHA256:
`b3033ee3369a18600a6ef3dce8b9e83b6165bbaea86d253d59db5268c428e0ea`.
Check SHA256:
`a033d964cb70b3d18c3a26a75915a82997b6ff376dce0daa6dc33128a85b02d7`.

u3458/d2b97285040f4616957e2e4dfad2e8f0 exited0 in its retained call. Exact72
excavation queries match the old owner. Generated bricks7→0, resident material
bytes28672→0, height samples1793→73, cave metrics11958→0 for this query set.
The omitted cave evaluations were unrelated cells in discarded full bricks;
13 additional signed/cave/world-edge probes compare against the original owner.
Actual bulk snapshots, caller snapshot mutation, two edits, eviction, canonical
restore and invalid-input rejection pass. `proof-v1.json` retains results and
all six source hashes. None of these reads advances physics or edits terrain.

Observed old/new sparse-pass times:182.334/6.474ms wall and93.758/3.469ms process
CPU. This is one cold comparison on a shared host with other work present,
not a robust throughput distribution or world-size claim. The reduction in
generated work is exact; a warm bulk workload may prefer the retained brick API.

u3460/3d639367a390403bb2698867b83ba070 Fallow inspect exited0. Root read its
actual JSON: no dead/cycle/clone findings in this two-file scope; seven static
estimated/no-imported-coverage health advisories remain. Existing edit has
cognitive18; the new point operation is below the reported thresholds. Upstream
dependency code is outside that scoped scan, and no clean-health claim is made.
The accepted predecessor and full tool output remain preserved.

Independent source review and the actual sparse-validation adapter consumer
are now assigned. No adapter-level timing or unchanged physics outcome is
claimed until that owner returns its one bounded comparison.

## Actual consumer closure, 2026-09-09

Root read the independent point-consumer RESULTS and complete six-site diff.
One u3474/7026978d16f64d098bf9177ba891ff8a passed16aggregate checks/15phases.
Actual moving-result and resumed-result files are byte-identical to the
accepted600s excavation outcomes, including canonical state and work.
The public72-read pass generates0bricks/0resident bytes/73height samples
including restore, with0cave metrics. Whole adapter advance was90.05ms
wall/97.31ms process CPU; validation3.18ms wall, all single fixed-order
observations. No paired cross-run speedup is claimed.
All33prior plus8candidate pins remained exact; candidate source is frozen.
Consumer source and evidence: ../../soil-water-v1/excavation-v1/point-consumer-v1.
This closes the experiment consumer; tracked game integration remains with
Delivery and was not requested merely to expose recorded frames.
