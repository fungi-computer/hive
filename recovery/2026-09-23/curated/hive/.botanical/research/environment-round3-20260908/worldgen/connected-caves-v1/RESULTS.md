# Connected cave component: actual first result

Root implemented and read the new component and immediate generated material/save callers. Independent Astra read the first shape before the proof; the exposed vertical-coordinate validation correction was applied. Fixed proof run-u2991.scope, invocation80ed5fcfc7074b1f8c1be7064f4baa43, exit0. Five bounded groups passed; the Node harness reports676.86ms including source/file evidence I/O on this shared host, not a browser/world-capacity benchmark.

The two predetermined default-seed owner regions(0,4) and(-1,4) each have503 geometrically selected voxels connected through positive-area voxel faces from their entrance to the sloping gallery and chamber. Their geometry crosses7 and6 actual storage bricks respectively. Every selected member was read through the actual material/brick caller, and all other cells in those decoded bricks remained byte-identical to the older cave base. All sampled centreline junctions belong to the connected component. This is a generated pit/gallery, not a staircase or human navigation proof.

Reverse query order with a one-brick cache, full eviction, copied descriptor mutation, a physically filled entrance cell, real JSON file reopen, stale edit rejection, foreign feature-definition rejection and base-equal override removal passed. Negative coordinates and bounded feature descriptor residency are covered. The new composite world recipe rejects the old world's identity rather than silently migrating old terrain. Legacy cave noise retains its exact previous sampling namespace.

The feature's entrance column must clear the sea datum plus3 voxel levels. This does not promise that every gallery/chamber cell is underground or dry. The component creates geometry only; it initializes neither water nor finite air and settles no excavation physics. No tracked source, game map, UI, renderer or deployment changed. Game Delivery can integrate an inspectable accepted source candidate after the ordinary ownership handoff; source/world identity must be explicit.

Evidence is frozen in run-v1/{proof.json,terminal.json,world.json,features.mjs,voxel-world.mjs,qualify.mjs,CONTRACT.md}. The first-shape Fallow read found no dead code or clones in features.mjs and reported estimated no-coverage CRAP advisories; final touched-file inspection follows independently of numerical evidence.

Final actual Fallow inspections run-u2993 (features) and run-u2992 (generated-world caller) both exited0; each scope found zero dead-code findings and zero clones. features.mjs has five estimated no-coverage CRAP advisories, maximum cognitive complexity5. The inherited unchanged edit operation remains a cognitive hotspot18, and six other generated-world functions have estimated no-coverage CRAP advisories. This is a disclosed retained preflight/commit responsibility debt, not an all-clear audit; no legacy edit/source bytes or assertions were deleted merely to turn the report green.
