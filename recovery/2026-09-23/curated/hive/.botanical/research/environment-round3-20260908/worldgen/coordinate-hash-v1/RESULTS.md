# Exact same generated world; lower observed sampling cost

Root authored the candidate and actual caller. The independent soil reviewer
read the exact old/new source diffs and qualifier before execution. Its single
preservation correction now writes each completed timing sample outside timed
windows, so a later failure retains earlier work.

One fixed packet u3287 / a6fe5b1c73084c258f213362c6ff3dfa exited0; terminal
session45573 was retained to completion. 53,458 assertions cover the independent
old string hash (including UTF-16, signed decimal boundaries and fallback),
three seeds/15x15 coordinates/five footprints, coarse map arrays, and all bytes
of12 surface/cave bricks. Real edit/eviction, both save-codec directions and
actual file reopen preserve canonical identity and data. Assertion count is
mostly repeated comparisons, not53,458 distinct physical scenarios.

Root found a coverage limit after reading the actual stats: none of those
regions exercised the added capsule-cave membership. A separate focused
u3294 /969a19d22747489399e4ca697790d28f exited0 over the two pre-existing
accepted present regions. All30 bounding-box bricks and both descriptors match;
actual membershipQueries21807, segmentTests48208, chamberTests15610. It did not
repeat the timing packet. Both old results and this explicit coverage distinction
are retained.

## Performance observations

One warmup per implementation, eight alternating-order rounds per equal fresh-
owner workload, deep equality outside the timed windows. Nodev24.20.0 on this
shared Linux/x64 EPYC-Milan host:

| Work | Old median | Candidate median | Candidate / old |
| --- | ---: | ---: | ---: |
|4096 exact terrain queries|250.77ms|143.81ms|.5735|
|8 cold volume bricks|144.40ms|63.36ms|.4388|

Terrain timings were noisy: old68.77–314.98ms, candidate46.08–203.24ms; one
paired round favored the old source. Cave-brick rounds all favored the candidate.
These are descriptive interleaved observations, not confidence intervals or
guaranteed browser latency. The change eliminates common coordinate-string
construction by source inspection; garbage bytes were not measured. Final whole-
process RSS250,769,408bytes includes both implementations, comparison objects
and the runner; it is not candidate-only memory. World residency/counted
geometry work and all generated values are unchanged.

## Source review and delivery boundary

Syntax u3285/c54baf77 exit0. Target Fallow u3286/fac0cecf exit0: no target dead
exports/files/cycles/clones; integerHash retains one estimated-coverage advisory,
cognitive5/cyclomatic6. No suppression or claim of measured coverage. Inherited
whole-sampler complexity remains, outside this narrow helper correction.

Main proof SHAfa3d0774007781596c33cd84bfc0bfecc0eed5f94b1362c59780ea7f7f865447;
helper SHAeae43f3c20f871dca2cd39d7f7c495334eda51b3a17dec35f0739b1dfdd18e24.
Source inventories retain every actual executable hash. The old heightSource
SHA inside semantic world identity denotes baseline recipe provenance; it is
not the hash of these optimized files. Existing saves remain the same recipe.

The current tracked terrain source differs from the frozen source only by
formatting/comments. Root read that full diff. integration.patch targets its
exact baseline and adds only the currently consumed2D helper exports; the
accepted3D candidate remains ignored until a real tracked volume caller exists.
Common helper/function bodies are unchanged. Delivery owns applying/reviewing
that patch, the actual browser worker/import consumer, existing map contract,
build and same-preview publication. No source/build/deploy claim is made here.

This improves one generation bottleneck. It does not finish biomes, refined
rivers, physical initialization, activation, fluids or playable world capacity.
