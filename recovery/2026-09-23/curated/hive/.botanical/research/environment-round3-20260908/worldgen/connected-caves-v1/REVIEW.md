# Independent first-shape source review

2026-09-08. Source-only review before the proposed proof. No source edits or numerical run by the reviewer.

**The architecture and geometry shape are coherent for the bounded experiment.** Proceed to the fixed proof after addressing or explicitly narrowing the returned column predicate's vertical-input contract below. This is not numerical or visual acceptance yet.

The actual diff against `volume-query-v1` adds the feature owner inside the existing `columnAt`/`baseMaterialAt` path. Point reads, brick generation, sparse-edit normalization and restore validation therefore share the same composite material rule. There is no independently applied carve overlay. The feature can only add void; unrelated old cavity decisions retain the exact old sample namespace. The combined generated recipe changes to v3, while the unchanged schema-2 sparse-codec structure remains appropriate. Full identity checks correctly prevent silently loading old-world saves as the new recipe.

Capsule and chamber tests operate on physical voxel centres: horizontal metres and 0.54 m vertical increments. Centreline offsets are logical-cell offsets converted once. Quarter turns rotate both the path and horizontal ellipsoid radii. The fixed radius is positive and the defined segments have nonzero length. Continuous overlap alone is not claimed as discrete connectivity: the planned proof enumerates sampled members, checks six-face connectivity and verifies that every such member is actually air through the world decoder. Because those members form a connected subset of actual void, additional legacy-cave void cannot invalidate the demonstrated path. The pit is explicitly not a human walking route or staircase.

A candidate sits near the centre of its 128 m region with bounded jitter. `geometryFor` verifies the entire capsule/chamber bounding box remains inside that region and the world's vertical extent. Consequently the one-region column query needs no hidden neighbor-region search. The region cache contains at most16 descriptors; exported descriptor snapshots cannot mutate its geometry. The cap is a cache-entry cap, not a cap on caller-retained snapshots or column closures. World decoding retains each column query only for its vertical loop.

**Small admission gap:** `column(x,z)` checks integer horizontal coordinates and owner-region bounds, but the returned predicate does not check `y`. Fractional y is sampled as an extra position and an out-of-bounds y returns false, rather than following a voxel-query contract. The current world path already validates integer in-bounds cells, so this does not invalidate those callers. If this exposed feature API is a voxel-centre query as documented, validate safe-integer y and the declared vertical extent in the predicate; apply the same policy to the absent-feature fast path. Alternatively document it as a geometric sampler, but do not imply it has the world's cell-admission behavior.

**Scientific limits to retain:** the dry-elevation margin is checked at the entrance column only. It does not prove that the whole gallery/chamber is below neighboring terrain, above groundwater, dry or isolated from other surface openings. The proof's below-protected-layer check is relative to the entrance bed, not every traversed local bed. Actual new carved cells and the entrance's direct connection remain useful outcomes under these limits.

The two regions and seed are fixed before execution; absence remains a fixture failure rather than triggering an unbounded search. The proposed proof compares all decoded cells in touched bricks against the old owner outside feature membership, checks actual point/brick agreement, reverse request order, eviction, snapshots, foreign identities and a real file-reopened edit. Filling one entrance-designated voxel proves that cell's persistence, **not** that the multi-cell pit becomes sealed. No fluid, gas, traversal or game-state authority is inferred from that edit.

No second renderer, live integration or expanded game scope was introduced. Gas-review priority remains unchanged.

## Final bounded acceptance after the owner's fixed proof

The vertical admission gap is closed in both ordinary and empty-column paths: `checkY` requires a safe integer inside the half-open world extent before membership is sampled. The reviewed current `features.mjs` hash is `b91219b568be736243b80fd16af35473587d3b7ada5819fd51ac2efde1590f48`; it is byte-identical to the retained run source. The world caller remains `68dcf790fc56b83cf661ad2a78294860809d98648a321d2c60fa3a4340d54112`, and the updated qualification caller is `b9274575e7f160e2650c68d0c4662d1f80d1b6077e8d656d1231e1f84525a00d`.

I read the actual `run-v1/proof.json` and terminal record: scope `run-u2991.scope`, invocation `80ed5fcfc7074b1f8c1be7064f4baa43`, exit0, five passed groups, 676.864 ms measured proof time. Proof SHA256 is `24b997e1a1fa9557bc49a0ab9c4d4b70d56066671bfd13bec60e52ec8e291958`. Each of the two preselected signed-region features has503 sampled members and503 six-face-connected cells, crossing7 and6 storage bricks respectively. Each adds482 actual void cells beyond the old base. All52,242 inspected non-feature cells preserve the old material result. The bounded packet also records reverse request order, cache eviction, copied descriptor ownership, strict coordinate/identity admission, and actual file-reopened entrance-cell fill/reversion.

**Accepted as a bounded connected-cave generation and persistence reference.** The entrance-only dry-margin, voxel-centre membership, non-walkable pit, two-fixture sampling and absence of liquid/gas initialization remain explicit limits. This is not proof that the complete cave stays beneath terrain, that filling one voxel seals it, or that a world population of caves has been qualified. No reviewer rerun was performed.
