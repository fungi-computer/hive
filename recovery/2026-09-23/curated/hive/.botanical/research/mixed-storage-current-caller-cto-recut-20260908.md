# Mixed storage: current caller and art recut

Game CTO source review, 2026-09-08. Root personally read current model/material, construction, jobs, activity/persistence and shelf-art callers, supported by a bounded native read-only review. HEAD is `9c16db8` with the v7 candidate dirty; Delivery retains its final proof/release and deconstruct-pm remains its sole correction writer. Nothing here expands that release gate.

This updates [the earlier mixed-storage readiness note](mixed-storage-consumer-ready-review-20260908.md) and reconciles it with [the first-brew vessel review](first-brew-vessel-and-process-cto-review-20260908.md). It is the next-consumer handoff, not a claim that mixed storage has shipped.

## Reconcile the actual source before implementation

- `ContainerSpec` already has `accepts: Material[]`, and the opaque transfer owner no longer goes through an `ownerMatchesRequest` function. Do not reimplement those old corrections. Actual shelf policy still accepts one mugwort, and capacity is currently a simple quantity.
- The real remaining special cases are in `jobs.transferOption`, the `shelf-store` completion decision in activity, and persistence's validation of the two current owning job/request shapes. Remove those restrictions as the generic Store consumer replaces them; moving the branching into a new string whitelist would not deepen the module.
- Source selection/reservation/pickup still require ground lots. Reserved transfers record a lot ID/quantity, but no resolved origin. Container release only discovers incoming destinations. These are real extension points for stored-wood withdrawal.
- `resolveMaterialDestination` already knows the actual site/container lifecycle. Jobs still repeats prefix/site resolution during planning, reservation commit and carrying continuation. Reuse one endpoint query through those callers when splitting the current `transferOption` hotspot (Fallow cognitive 39); do not add another giant container-withdrawal branch.
- Current v7 is explicitly fresh-only. The old instruction to preserve v1–v7 reads is superseded. Do not reopen v1–v6 decoders as part of storage.

## First working source shape, followed by the actual playable outcome

Keep materials as the sole location, reservation, carry and capacity mutation owner. A reserved source gets one closed resolved origin: ground cell or container ID. At pickup, require the source lot still occupies that origin; compute legal approach from the current endpoint. No copied contents list or writable source coordinates follows a carried lot.

Extend the existing container endpoint query by operation. For the first consumer, deposits can target unfinished construction buffers or finished shelves; withdrawal can target finished shelves. Construction inputs and finished embedding are not general stock. The query returns the actual current site/container so current movement computes its work approach. This does not require a new generic provider registry, container-role hierarchy or scheduler before the first shelf works.

Split demand interpretation from common transfer-candidate construction. A build planner chooses required material/remaining quantity and eligible source portions; a Store planner chooses the requested lot/amount and storage destination. Both use the same source/destination access checks, reservation commit and carrying continuation. Preserve libcolony matching, priority and personal/shared/Work semantics. Candidate evaluation must not acquire a real claim or move goods.

The supporting reader's deliberately narrow first checkpoint stops at source-origin/withdrawal mechanics. **That alone is not the player-visible mixed-storage exit.** Finish the actual typed Store ingress for both wood and herbs, quantity/target selection, grouped contents/capacity inspection, and corresponding main/HUD callers. The current herb-only button cannot put wood on a shelf. Avoid keeping a second herb-specific job/haul implementation behind a new general menu.

For capacity, Delivery should settle one explicit small policy from the existing proposal: six abstract shelf spaces, wood costing two and an herb bundle one. These values remain a proposed balance until Delivery records its decision. Existing safe integer quantity and per-item carry/divisibility rules remain separate. Construction buffer demand must still use its actual recipe quantities rather than inherit shelf bulk units. No liquid is stored without a vessel.

Reserved outbound portions continue to occupy their source until pickup. Teardown of their source shelf releases those unpicked claims and associated work before removing/ejecting contents. Already-carried withdrawals are no longer inside that shelf and continue normally. Full/self/unreachable transfers fail without leaked claims; do not credit future outbound space to make swaps appear possible. Paused commands can admit intent; paused movement and work do not advance.

## Reuse the already accepted shelf art

The chunky-firewood correction is already tracked at `src/art/mixed-shelf.js`, SHA256 `0cec741a743c0d4bf9498eba9d85b420376f351a2af2c0e78088c88a9fc52e3e`. Its acceptance/source/evidence are in `.botanical/mixed-shelf-firewood-20260908/HANDOFF.md`; historical study release `29cb9b9` and deployment `848470e2-47bf-4611-a5ec-80a9d22b8fde` recorded HTTP parity, not hosted browser interaction. Root re-opened its accepted native sheet and verified the current file hash. No new firewood art pass or second art gate is required to use unchanged bytes.

The ten existing profiles depict up to three visible wood/herb representatives in two directions. They are not capacity definitions. A proposed six-space shelf can contain six herbs or two wood plus two herbs, which exceeds three visible representatives. Choose the profile from actual contents, preserve both material types when mixed, and clearly report undisplayed quantity. The inspector remains exact (`Wood ×2`, `Mugwort ×2`, occupied and incoming bulk), while a compact overflow indication explains omitted representatives. A nonempty shelf never selects the empty profile.

For this bounded two-material consumer, reuse the finite profile bank and bake each shared profile once. Do not bake on every inventory change or allocate a texture bank per shelf. Bind picking to the actual chosen profile and current direction. The current renderer's boolean `some(mugwort) -> filled` and `Mugwort n/1` inspector must be replaced at this join.

A future third material such as a portable keg must not trigger a combinatorial bank for every possible inventory. At that consumer, use an intentional shelf-back / contents / shelf-front composition and named display positions under Astra art review, or another bounded representation with honest quantities. Internal shelf-part ordering does not solve global object depth. Do not build that composition framework now merely to avoid using the accepted two-material art.

## Version the actual released sequence

Mixed storage adds source-origin and capacity/Store semantics before brewing. Consequently the earlier preselected statement “brewing writes v8, reads v7/v8” cannot remain an unconditional promise. Delivery assigns the next schema to each actual released wire change. If mixed storage ships v8, brewing is later v9 (or later if another changed schema ships first). This supersedes only the prematurely allocated version number; preserve the intended valid-predecessor migration, exact paused restore and raw recovery behavior.

For a valid v7 reserved transfer, its supported origin is the currently referenced ground lot; migration can derive and normalize that origin after validating the original relations. Carrying transfers keep their hand location and obligation; IDs, quantities, job progress and allocator do not change. Larger shelf capacity does not create contents. Reject malformed predecessor saves atomically, preserve their raw data and avoid load-time overwrite. The eventual cache/spring initialization belongs to the brewing feature's actual predecessor version, once only, not every Continue.

Do not add future migration code to the current fresh-v7 release. Equally, do not treat today's deliberate v1–v6 reset as an eternal license to discard later valid worlds.

## Useful exit and custody

The next complete storage outcome is one real shelf receiving wood and two distinct mugwort bundles, then supplying a normal wall from stored wood through common transfer. Show exact contents/capacity, last-space contention, a paused/reloaded live withdrawal, and source teardown before versus after pickup. Preserve wood and herb accounting, stable lot identity and user intent. Use focused source laws and a short actual-input trace, not the whole home-construction marathon.

Floor stockpiles remain the next separate storage outcome under the accepted zone policy; choose their exact ordering against portable vessels at the post-mixed-storage handoff. They do not need a backpack puzzle or conveyors. Dwarven automation remains later and does not gate this shelf.

Delivery owns the ordinary capacity/ordering/version decision, visible PM assignment, sole coupled writer, reviewer corrections and serial same-preview integration. Root retains any genuinely changed art and deeper architecture decision. This recut reconciles existing plans so the next writer can produce playable storage; it creates no extra approval checkpoint.

## Post-v7 consumer review

The common transfer release is now `857f50416ab092f4fa5e107577795136f5318efd`. Root read the actual `local-final/proof.json` and `hosted-parity-final/proof.json` under `.botanical/common-transfer-v7-final-20260908`: the former owns the physical wood-to-shelf and mugwort-to-storage interaction plus exact material reload; the latter owns identical hosted index/assets, not another gameplay trace. Root also personally viewed `local-final/stored-common-transfer-v7.png`. Mixed storage remains the next consumer, not part of that release.

That frame exposes a concrete presentation defect to correct when the mixed-storage inspector joins. `src/style.css` gives `.window.target-window` both a pointer-derived `top` and the rail `bottom`, stretching a short contents card into a large empty panel. Use content-sized placement bounded by the actual viewport and rail; when contents grow, keep controls reachable and preserve focus. A stored lot should show its holder and useful contents/location information, not stop at the implementation-oriented "This bundle is not on the ground." This is a game-owned HUD/layout correction within the actual mixed-storage caller, with Caps styling retained. It does not require shared Caps source custody or another art decision.

Current candidate queries also have a concrete repeated-scan shape: `availablePortions` iterates lots and calls `availableQuantity`, which finds each lot again and scans transfers for reservations; the build planner then finds each selected lot again before routing. Read-only candidate preparation can compute reserved quantities once and obtain available lot facts without repeating those point queries. Keep actual reservation authoritative and revalidate at commit: a snapshot is neither permission nor a promise, and two accepted candidates may compete after it was computed. No persistent index is required merely to implement withdrawal.

Several shelf lots will share one physical source/work endpoint. If route reuse is needed, scope it to an unchanged candidate-planning snapshot and key the actual source, destination, operation and topology. `beginWalk` currently retains the supplied path array and `walk` mutates it with `shift`, so never share one mutable cached path between actors; clone on ownership transfer or choose an immutable result contract. Counts and timings from a material-query microbenchmark do not establish that routing dominates runtime or that a population target is supported. Preserve exact libcolony costs and stable tie behavior while removing redundant work.

The bounded measurement at [material-query-cost-20260908/RESULTS.md](material-query-cost-20260908/RESULTS.md) uses an isolated exact `857f504` materials source snapshot under Node 24.20.0. Warm median `availablePortions` time with one reservation per two lots was about 0.004 ms for 20 lots, 0.278 ms for 200 and 26.8 ms for 2,000. Returned quantity/checksum assertions passed. This is a synthetic material query, not a valid large Clearing, an actor benchmark, a browser frame measurement or a forecast for the current eight-tree map. It supports removing the demonstrated repeated lot/transfer scans at this consumer; it does not justify a new ECS or persistent cache. The final owned `run-u2025.scope` exited 0; failed launcher/live-hash attempts remain recorded.

### Correct shared-work serialization before accepting the next candidate

Root and an independent bounded source reader found a P1 introduced by `857f504`, compared with `9c16db8`. In shipped `jobs.ts:312–337`, each idle actor offers only its first eligible job and then breaks. All offers are collected before assignments commit. With two idle, undrafted, Chop-enabled home members and two reachable shared oak orders, both offer only the first job. `matching.ts` derives its tasks from those offers, so the actual optimizer receives two actors and one task, not two alternative jobs. One member necessarily remains idle. Because `workDirty` was cleared and partial commitment does not wake it, that member can remain idle through the other member's travel and eighty chop ticks until completion or another real wake.

The previous scheduler offered a bounded set of distinct shared jobs jointly and rearmed after partial commitment (`9c16db8:jobs.ts:508–552,571–572`). The shipped personal-first-plus-shared test gives the second actor a different scope and therefore misses this case; the scarce-source law correctly expects one transfer and also cannot establish parallel work.

Delivery must route the correction through the existing `jobs.ts` writer: restore bounded joint shared offerings and lawful retry after a partial commitment, preserving personal-order priority, Work preferences, cargo continuation, stable costs and authoritative material revalidation. Merely deleting `break` is not a complete personal-order policy; retrying unchanged offers every tick is not a complete assignment policy. Add a focused actual-WASM law showing two shared reachable Chop jobs assigned to distinct eligible home members in the same scheduling pass, with existing personal/scarcity laws retained. Source establishes this defect; no browser reproduction or full-home rerun is claimed or required by this finding. Delivery chooses the recoverable correction/release checkpoint under sole Git custody.

Delivery subsequently reported the new real-WASM two-shared-Chop law green in `u2044` (15 clearing laws), with typecheck `u2045` and diff `u2047`. Root read that actual test and candidate diff: it proves distinct simultaneous assignments for the demonstrated case, but is not blanket preservation of scheduler policy. The common `offer` helper still applies routine Work preferences to personal offers, contrary to the retained explicit-personal-order bypass; and match commitment still sorts actor ID alone rather than original job order then actor ID. Return these through the same writer, with personal Chop while routine Chop is disabled versus excluded shared Chop, and scarce-source commitment honoring earlier job order independent of actor ID. Review per-actor shared windows against the previous per-party bounded frontier so widening does not silently change queue precedence. These are current candidate/source findings, not additional browser or art gates and not a claim that their corrections are already deployed.

### Correct the predecessor from released wire data

Later root source inspection found a concrete positive migration failure in the mixed8 checkpoint reported green at u2093/2094/2095. Exact `git show 857f504:src/model.ts` defines saved `TransferJob` as `kind:"transfer", source, destination`; its `src/persistence.ts` strict job schema agrees. `store-herb` with bundle/shelf is the old UI command and commands are omitted from a snapshot. The candidate `migrateV7State` instead converts only `job.kind === "store-herb"`. A real schema7 save retaining an outstanding storage job therefore keeps `kind:"transfer"` and fails the current schema8 job union. The new test named “only former Store shape” constructs a fictional saved command-shaped job, so its success does not prove compatibility.

The same candidate envelope declares old `savedState:z.unknown()` and validates only after normalization with expanded current rules. That permits current-only fields/forms mislabeled as7 and can overwrite a supplied malformed phase.origin rather than reject it. It also does not establish the old one-herb shelf bounds before accepting the larger mixed shelf. Parse the actual released7 shape strictly, check predecessor-specific relationships and capacity, then migrate `transfer` jobs to `store` retaining IDs/source/destination and derive only the genuinely absent reserved ground origin. Reuse common relational helpers with explicit predecessor policy where faithful; do not copy an unrelated legacy decoder or reopen1–6.

Root sent both exact source findings to Game Delivery for the same writer/reviewer before publication. Required focused evidence is genuine7 reserved and carrying storage-job restoration, plus rejection of current8 job/source/origin shapes mislabeled7, unknown predecessor fields and old overcapacity. Preserve the raw invalid value. These are source findings, not a claimed executed reproduction or another browser gate. Scheduler preservation is now separately corrected in the same candidate: personal Work bypass, queue-global shared frontier, job-order then actor-ID commitment and copied actor paths were personally re-read; no new deployed revision is claimed.

### Personal Fallow disposition at this checkpoint

Root read Delivery's actual `.botanical/mixed-storage-v8-final-20260908/fallow-audit-mixed8.json` (SHA2568f1bedc794ef207a1e0f26b7a9ae0334f5818ad67d0e0ed3dcca80d78541acf2) and `fallow-health-mixed8.json` (SHA256cac4f133e443c5b42175cb22925794434b09f22556b83089de1d831c3195f393), then the implicated source and immediate callers. Fallow3.20.0 reports a dirty40-file scan against HEAD857f504, not an immutable clean-release audit. Audit verdict is fail: four dead-code/dependency/cycle findings,30 complexity findings and three clone groups; attribution marks two dead-code and five complexity findings introduced. Health analyzed32 files. Coverage/risk numbers are static estimates, not measured test coverage or performance.

The two new unused exports are `originForLot` and `availableLotFacts`; their functions have real internal callers and should remain private, not be deleted or suppressed. They are already private in the subsequently inspected candidate. Required packed Stipe and the known activity/routine import cycle are inherited; preserve the package and report the cycle. The three clone reports include repeated world-picker binding/render fragments and phase validation; some common syntax represents different phase laws. Do not blindly merge reserved-origin validation with carried-location validation.

The introduced responsibility hotspots are `validateReservedTransfer` (cyclomatic30/cognitive20), `validateTransferOwner` (20/16), `constructionTransferOption` (19/24), the migration mapper, and the small shelf-profile helper (5/4,24 lines). The latter is a bounded art-profile choice, not a reason to add a generic composition framework. Separate predecessor admission from conversion when fixing the real save defect; keep common transfer-candidate preparation separate from build demand. No broad rewrite or zero-warning release claim follows from these metrics.

Before adding vessel/process UI, deepen the existing target inspector by target responsibility: `Target` is332 lines/cognitive59, and `runAction` is306 lines/cognitive32. The current lot branch already repeats container bulk calculation in JSX. Existing material selectors should derive exact contents/availability once; a material inspector should consume those facts and submit commands, with character/site/plant views as separate components sharing the Caps window frame. Keep actual source admission authoritative. This is a concrete next-consumer boundary, not an instruction to block mixed storage on a whole-HUD rewrite.

The inherited `advanceWork` (82 lines/cognitive45) joins target resolution, arrival, access checks and effect completion. Adding held-use by extending its nested transfer/target ternaries would worsen that responsibility mix. Its next consumer needs one resolved operation/access result, the existing movement step, and exhaustive activity completion handlers under the current owner.

The activity/routine cycle is caused by pure `DAY_TICKS/hour/isNight` queries living in `routine.ts`, while routines call activity cleanup. Actual consumers are activity, routine, view and HUD. At the next safe shared-caller handoff, moving the unchanged queries into a small world-time module and updating those imports can remove the cycle; preserve all formulas, the one committed tick and current pause behavior. Do not introduce another timer or scheduler. These concrete module improvements are follow-on work for Delivery alongside the real vessel/process consumer, not additional current browser/art gates.

### Focused browser proof source review

Root and one bounded independent reader inspected `scripts/prove-mixed-storage-v8.mjs`, observed SHA256 `1baf66cc096e67daf2b2f99797080ad63e5912ddba766d93113db2d21a78dfdb`, without edits or execution. The completion predicate around line673 can pass an absent wall (`find(...)?.finishedAt !== null`) and can miss a leaked transfer because it looks up the build job again after normal completion removes that job. Require the actual finished wall and absence of transfers using the already captured original job/transfer IDs, then assert those facts before recording `transferSettled:true`. Delivery received the concrete source correction; its replacement owned run had already begun, so this note neither stops it nor requests another long run. Evidence produced by older script bytes must retain that limitation.

No debug-state mutation bypass was found: the state getter clones and actions use physical controls. The script covers fresh schema8 and reserved-phase exact materials/jobs/task reload. It does not provide browser evidence for schema7 migration, carrying-phase reload, overflow rejection or incoming-capacity contention; separate source/unit laws own those claims. The first retained local attempt stopped at a nonexistent `#task` planting control after earned oak wood and finished construction; `errors:[]` does not turn that failed attempt into completed mixed-storage proof. Delivery corrected the selector separately and retains proof, source and release custody.
