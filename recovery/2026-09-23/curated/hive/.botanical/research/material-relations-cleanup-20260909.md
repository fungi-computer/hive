# Material relation responsibility split — 2026-09-09

Only source changed: `src/engine/materials/relations.ts`. SHA-256: `4757fd9856b3e933a10b9a694b0ca67a5b13ca3c8d80f01012cbf44d54d34963`. Frozen for independent review. No schema/public API/material mutation changes, no changes to other source, no Git/build actions.

Read actual recut1 Fallow findings: `validateMaterialRelations` CC43/cognitive66/136 lines and `validateBindings` CC34/cognitive55/98 lines. The report is retained at `.botanical/research/material-engine-20260909-recut1/fallow.json`. Its zero-coverage CRAP severity is an estimate, not measured coverage of executed laws. No new Fallow scan was run under the instruction to run only existing focused laws, so there is no claim of measured new scores or advisory-green status.

The entrypoint now shows the original stable sequence: resolve valid lots/portable endpoints; validate binding/receipt lifecycle; validate transfer ownership and accumulate non-overlapping reservations; check each lot's aggregate claims, placement and hand custody; reject embedded inventory/promises; check capacity. This is one physical relation owner and still calls the exact runtime lot, transfer-phase, bound-quantity and capacity rules. No private choreography escaped through a new public interface.

`validateBindings` owns ID uniqueness and station exclusivity, dispatches ordinary-use versus recipe relationships, then validates transformation lifecycle and output receipt budgets. `validateUseBinding` checks the physical claim type. `validateRecipeBinding` owns pre/post-prepare inputs, retained lots and promised endpoints together. `validateTransformationLifecycle` makes active versus settled binding ownership local; `validateOutputConsumptions` owns the aggregate finite role budget. These responsibilities were interleaved in one loop-heavy function.

`validateTransferIntent` owns delivery endpoint versus ordinary-use binding joins. `validateTransfers` retains actor/owner uniqueness, shared phase validation and the exact use-reservation overlap deduction. `validateHandLot` owns unique actor custody and the positive binding-only hand case. `validateLotCustody` retains the per-lot order: overbooking first, container placement next, hand custody last. Embedding invalidation and capacity each remain a separate whole-state invariant.

Validation order/messages preserved: container uniqueness precedes lot uniqueness; each lot validates before its portable interior is merged; binding ID/receipt uniqueness precedes bindings in saved array order; recipe station and roles precede live input/retained/promise checks; all binding checks precede transformation/receipt checks; each transfer's uniqueness/phase/intent precedes its reservation accumulation; each lot's aggregate/placement/hand checks finish before moving to the next lot; embedding uniqueness and contents precede capacity. The pure transformation map lookup moved into a helper argument, without changing observable checks or failure order. Removed only an unused local name for the still-executed lot uniqueness check. Local parameter typing is explicit; additional function/type lines buy actual invariant boundaries, not a lower file line-count claim.

Existing focused laws completed exit 0, 130/130: `run-u3836.scope`, invocation `c2dca28914f54006aa7f44ff47c93e4f`. Command through mandated run-proof wrapper: `node --test src/engine/materials/materials.test.js src/materials.test.js src/needs.test.js src/persistence.test.js src/clearing.test.js`. Output retained at `/tmp/hive-material-relations-cleanup-laws.log`; session 41712 polled through completion. This covers public engine snapshots, bindings/recipes/held-use, malformed current and historical saves, actual care overbooking, construction/shelf and real-WASM consumers. No new tests/suppressions, typecheck, browser, server or performance claim.
