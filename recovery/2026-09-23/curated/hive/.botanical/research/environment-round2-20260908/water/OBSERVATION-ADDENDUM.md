# Additional observation declared after the 600 s results

The original 600 s downstream-deprivation threshold remains failed. The original downstream source can satisfy the capped 0.06 m³/s user while losing otherwise unused stock into the pond. A separate 1200 s observation will retain the same geometry/initial stock/demand and no numerical threshold adjustment, to locate the first sustained shortage after initial arrival. This is a diagnostic continuation of finite supply, not a replacement acceptance test.

Separate single-process 600 s cost observations use the same 32² case, no snapshots while advancing, and report wall time, canonical typed bytes, declared scratch typed allocations and process RSS. Shared host load and JavaScript collection are uncontrolled; no hardware/population performance claim.
