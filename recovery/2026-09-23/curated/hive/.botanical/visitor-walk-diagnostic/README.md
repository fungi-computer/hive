# Visitor walk determination, 2026-09-07

The reported `[3,3,5,3,3]` from run-u820 does not establish missing original
poses. Its phase label and displayed image sources were captured in separate
browser calls. The study renderer updates those together in one synchronous
render function, but a ticker update can occur between protocol calls.

The inspected uncommitted proof also changed the committed expectation from four
visitors with exactly five distinct poses and Sedge with at least five into all
five figures with exactly five. Sedge's accepted phase-shifted hair motion makes
that new symmetry requirement incorrect. No figure geometry or game caller is
changed by this correction.

`index.html` invokes the actual current `figure`, `bake`, and study camera at
48×64 pixels for five visitors × eight phases × four facings. It does not
reimplement the geometry or introduce a production art path. Static extraction
produced `[5,5,8,5,5]` in every facing: Rowan, Moth, Sedge, Pip, Nettle.
The duplicate-frame equivalence classes and before/after source hashes are in
`evidence/proof.json`. The figures hash is
`a5911f99b0608bc7bb41a9684886ac69fef6e9b24a727b2f99c2a387dd1fc5c4`.

The diagnostic ran through the ordinary proof runner as run-u828.scope,
invocation f30ad4cfb7cd4f3e9a54293389df9f96, native session 90215, normal exit 0.
Astra personally inspected the native phase sheet and 2× rendered comparison.
The short recorded motion is retained; this statement does not claim an uncut
video review or a successful rerun of the full study suite.

`sampling.patch` changes only the visitor proof: it captures phase metadata and
image sources atomically and asserts the measured `[5,5,8,5,5]`. The five-figure
distinctness at each phase remains required. A bounded native Terra source review
accepted the patch and the current-bake basis. Game Delivery owns its application,
formatting, and one corrected ordinary study proof; no other source custody moves.

Diagnostic run command, with the existing dev server on port 5187:

```sh
/home/levi/src/Botanical-next/.agents/skills/orchestrate-multi-lane-work/scripts/run-proof.sh node .botanical/visitor-walk-diagnostic/prove.mjs
```

Preserve run-u820 and all previous evidence. Do not replace the approved hair,
tool grips, or work poses to satisfy the discarded all-five symmetry assertion.
